import { CompoundInterestDirective } from './compound-interest.directive';

describe('CompoundInterestDirective', () => {
  it('should create an instance', () => {
    const directive = new CompoundInterestDirective();
    expect(directive).toBeTruthy();
  });
});
