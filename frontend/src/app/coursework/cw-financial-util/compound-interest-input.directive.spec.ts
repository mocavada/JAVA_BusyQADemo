import { CompoundInterestInputDirective } from './compound-interest-input.directive';

describe('CompoundInterestInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CompoundInterestInputDirective();
    expect(directive).toBeTruthy();
  });
});
